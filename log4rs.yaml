refresh_rate: 30 seconds
appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%H:%M:%S)} {m}{n}"
#       pattern: "{d(%Y-%m-%d %H:%M:%S)} {h({l}):5} {M:35}[{T}] - {m}{n}"

root:
  level: warn
  appenders:
    - stdout

loggers:
  aceai_cli:
    level: info
