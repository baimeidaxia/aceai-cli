refresh_rate: 30 seconds
appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%Y-%m-%d %H:%M:%S)} {h({l})} {M}[{T}-{I}] - {m}{n}"

root:
  level: warn
  appenders:
    - stdout

loggers:
  aceai_dp_cli:
    level: info
